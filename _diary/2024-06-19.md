#diagram #plantuml #c4

```plantuml
!include <C4/C4_Context.puml>

LAYOUT_TOP_DOWN()
LAYOUT_WITH_LEGEND()

title System Context Diagram for Data Pipeline

System_Ext(review, "Code Review", "Code Review Metric System")
Boundary(system, "") {
	System_Ext(service, "Source Services", "Source of monitoring data")
	System(app, "Data Pipeline", "Backend System for ETL")
}
Person(developer, "Developer", "DevOps Team")

Rel_Right(review, app, "Retrieve changed data")
Rel_Right(review, service, "Retrieve real data for changed")
Rel_Left(developer, app, "Schedule, Deploy, Monitoring")
Rel_Down(app, service, "Collect monitoring data")
```

```plantuml
!include <C4/C4_Container.puml>

title Container Diagram for Data Pipeline

' DB, Web Server, Application, Redis

System(log, "OpenSearch", "Logging System")

```